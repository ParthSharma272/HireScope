# HireScope Backend — Quick Start

Requirements:
- Docker (optional) or Python 3.11+
- If running locally: install system deps for pdf2image & weasyprint (poppler, cairo, pango)

## Run locally (python)
1. Create venv:
